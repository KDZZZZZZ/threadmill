# Threadmill 验收计划（草案）

状态：v0.2。合成用例只做协议冒烟，主要结论来自真实项目；压力测试全程采集运行指标，性能阈值在首轮基线后校准。

## 1. 验收目标

确认 Threadmill 作为轻量级 Agent OS，能够在隔离工作区中稳定完成以下闭环：

```text
用户输入
  -> CLI / TUI
  -> 常驻 manager
  -> 协调图（planner -> executor -> verifier）
  -> Agent ReAct（模型 -> 工具 -> 模型）
  -> VFS / 命令执行 / 记忆图
  -> 子任务 spawn / join 或动态求助
  -> 任务报告、持久化、取消与恢复
```

允许调用真实模型和真实工具。真实调用必须使用临时工作区、唯一标记和可回收数据，不能把凭据写进仓库或验收日志。

## 2. 通过等级

- **P0 阻断项**：构建、单元测试、主链路、隔离、取消恢复、凭据安全。任一失败则不通过。
- **P1 核心项**：子任务、动态求助、记忆压缩、TUI、真实项目任务、性能与压力门槛。失败需有明确豁免。
- **P2 观察项**：极限规模任务图、8 小时以上 soak、跨平台体验。首轮可只记录结果。

## 3. 验收环境与证据

### 真实项目样本集

不以人工拼出的几个小文件代替真实项目。样本集至少包含 5 个固定 commit 的、可公开复现的仓库副本：

| 样本 | 规模 | 用途 |
| --- | --- | --- |
| Threadmill 自举 | 当前完整工作树快照 | 真实失败诊断、跨包修改、测试、Agent OS 自身能力 |
| 小型成熟 CLI/库 | 1k–10k 文件 | 快速 bug fix、配置修改、单包测试 |
| 中型并发服务 | 10k–50k 文件 | 跨包追踪、并发、取消、race、集成测试 |
| 非 Go 应用 | 10k–50k 文件 | 验证文件/命令工具和 Agent 编排不依赖 Go 项目 |
| 大型仓库或 monorepo | 50k+ 文件 | 搜索、上下文选择、长任务、调度和资源压力 |

每个项目至少准备以下真实任务中的 4 类：已有失败测试的 bug fix、跨文件小功能、行为不变的重构、只读诊断、配置/文档修改、两个可并行子任务、必然产生文件冲突的任务。优先从已关闭 issue 或已合入 commit 反向构造任务，把原补丁隐藏起来，只把验收命令作为 oracle。

每个任务清单固定记录：仓库 URL、commit、依赖版本、任务原文、允许修改范围、禁止行为、基线命令、验收命令、超时、token/费用预算和参考结果。质量不能只看“模型说完成了”，必须由真实构建、测试、lint、git diff 和人工抽查共同判断。

### 隔离与安全

- 固定 Threadmill commit、Go 版本、OS、CPU、内存；Linux 执行隔离场景需确认 `bubblewrap` 可用。
- 每次运行创建新的可丢弃仓库副本和独立 Threadmill HOME。失败后保存只读产物，重新测试时创建新副本，不复用被污染状态。
- 验收进程不携带 git push、云环境、生产数据库和消息发送凭据；网络只放行模型端点及任务明确需要的依赖源。
- 使用专门的验收凭据，`credentials.yaml` 权限为 `0600`，目录权限为 `0700`。
- 真实模型场景固定 provider、base URL、model、时间段。重复性能实验串行执行；压力场景只在用例明确要求时并发。
- 每个场景使用唯一 run ID 和 marker，便于核对输出、文件、记忆、进程和日志是否串线。

### 每个用例至少保留

- 命令、配置摘要、开始/结束时间、退出码。
- stdout、stderr、运行日志；日志中不得出现 API key。
- 模型请求次数、工具调用次数、token、首字延迟和总耗时。
- 验收前后的协调图、记忆图、progress、checkpoint 和文件视图差异。
- 最终 `git diff --stat`、完整 patch、项目自身构建/测试/lint 输出，以及是否需要重试。
- CPU、RSS、goroutine、文件描述符、子进程、临时目录和磁盘用量的时间序列。
- 失败时保留错误类别与可复现步骤，不保存敏感请求正文。

## 4. 功能验收范围

| ID | 优先级 | 功能 | 主要检查点 | 通过标准 |
| --- | --- | --- | --- | --- |
| F01 | P0 | 构建与基础测试 | 构建、全包测试、race | `go build ./...`、`go test ./...`、`go test -race ./...` 全部通过 |
| F02 | P0 | 配置与凭据 | `threadmill.yaml`、credential 查找、URL 校验、文件权限 | 合法配置可启动；缺项、明文项目密钥、不安全权限被拒绝；错误和日志不泄露密钥 |
| F03 | P0 | CLI | `-p`、`-C`、help、非法参数、stdout/stderr 分流 | 单发模式返回唯一最终答复；进度只进 stderr；退出码符合成功/参数错误/运行错误 |
| F04 | P1 | TUI | 输入、流式输出、任务活动、token、resize、Esc、Ctrl+C、`/quit` | 不重复渲染流式全文；状态正确；取消后仍可继续输入；退出无卡死 |
| F05 | P0 | Provider / ReAct | Responses API、SSE、文本、工具调用、工具结果回灌、usage | 完成“模型调用工具，再读取结果回答”；delta 顺序正确；最终答复不重复 |
| F06 | P0 | 文件与 bash 工具 | read/write/edit/ls/grep/find/bash、超时、输出截断、非零退出 | 工具结果可被模型继续使用；路径不能越界；超时能终止进程组；输出上限生效 |
| F07 | P0 | VFS 与环境隔离 | fork、snapshot、三路 merge、冲突、release、live/overlay 同步 | 父子/兄弟环境不可见对方未合并修改；合法增量合并；冲突原子失败；宿主基线不被意外修改 |
| F08 | P0 | 协调图主流程 | pending 图替换、planner/executor/verifier 顺序、报告 | 三角色各执行一次且顺序正确；verifier 输出成为任务报告；完成后进度被清理 |
| F09 | P1 | 子任务与动态求助 | spawn/join、requestHelp/provideHelp、非法成环/跨树 | 可并行的子任务被调度并 join；求助任务完成后原节点恢复；非法图不产生部分变更 |
| F10 | P1 | 记忆与上下文 | 订阅、organize、drop、compact、fork/merge、重启加载 | 相关记忆可找回；未订阅内容不注入；drop 不删图；compact 保留关键事实；环境间不串记忆 |
| F11 | P0 | checkpoint 与恢复 | 模型中断、工具前后中断、角色完成后中断、进程重启 | 已完成角色不重跑；未完成 ReAct 可继续；无重复任务报告；完成后 checkpoint 清理 |
| F12 | P0 | 取消与资源回收 | manager 抢占、任务树取消、命令进程组回收、临时目录 | 取消在时限内返回；子任务和 bash 子进程不残留；再次发送消息可正常工作 |
| F13 | P1 | 事件与日志 | model/tool start/delta/end/error、agent ID、token | 事件成对、顺序可解释；delta 不刷入常规日志；错误有 agent/tool 上下文且无敏感信息 |
| F14 | P1 | 真实项目交付 | 定位、修改、构建、测试、diff 质量、范围控制 | 结果由项目自己的验收命令证明；不改无关文件；不伪造测试；报告与实际状态一致 |
| F15 | P0 | 压测监控 | 延迟、流量、错误、饱和度、runtime、资源回收 | 监控缺口先补齐；压测期间可定位每次卡顿/失败所在阶段；异常触发告警并保留 profile |

## 5. 必跑端到端链路

### L01：CLI 到真实模型的最短链路（P0）

```text
threadmill -p
  -> manager
  -> Responses SSE
  -> stdout 最终答复
```

输入要求只回复一个唯一 marker。检查首字流式输出、最终内容、退出码、stdout/stderr 分流、token 和日志。连续 5 次至少成功 4 次；失败必须可归类为产品错误或外部 provider 错误。

### L02：真实 ReAct 工具闭环（P0）

复用已有测试：

```sh
go test -tags=integration -run '^TestLiveReActWithUserInputAndTool$' -v ./internal/agent
```

链路为“用户 marker -> 模型生成工具参数 -> echo 工具 -> 工具结果回灌 -> 模型只回复 marker”。要求至少两次模型 step、工具恰当调用、历史消息配对完整。

### L03：Threadmill 自举真实任务（P0）

把当前 Threadmill 工作树导出为可丢弃的独立仓库，要求运行中的 Threadmill 处理一个真实失败测试或边界 bug。manager 建立任务，由 planner 阅读相关实现和测试、executor 修改并运行定向测试、verifier 独立检查 diff 和复跑验收命令。

检查：

- manager 只编排，不代替角色执行。
- planner、executor、verifier 顺序及输入传递正确。
- verifier 能看到 executor 所在环境的结果。
- 修改命中根因、范围可解释；不得删除或弱化失败测试来制造通过。
- 定向测试和受影响包测试通过，最终 patch 可由普通 git 工具审阅。
- 最终产生唯一任务报告，包含耗时、token、outcome。
- progress 和 ReAct checkpoint 在成功后清理。

### L04：子任务、join 与动态求助（P1）

准备两个互不依赖的输入，要求两个子任务分别处理，再由父 verifier 汇总；其中一个角色在运行时调用 `coordination_requestHelp` 提交编排建议，manager 用 `coordination_orchestrate(action=provide_help)` 增加帮助任务。

检查并行启动、槽位限制、兄弟隔离、join 顺序、子记忆/文件合并、原任务恢复，以及同一求助不会被重复合并。

已有真实协调图测试作为基础：

```sh
go test -tags=integration -run '^TestLiveGraphRunMemoryOpsAndEnvVersions$' -v ./internal/coordination
```

### L05：跨轮记忆与压缩（P1）

第一轮写入唯一事实，第二轮通过 `organize_subgraph` 找回；随后用较小的测试 context window 触发 compact，再询问该事实。关闭并重新打开 manager 后再问一次。

要求四次答案一致；compact 后消息体减少但事实仍在；未订阅的另一条 marker 不得泄漏；drop-context 只影响本轮注入，不删除记忆节点。

### L06：取消、崩溃与恢复（P0）

分别在以下位置终止：模型流式中、工具调用前、长 bash 中、planner 完成后、子任务运行中。先用 Esc/中断做优雅取消，再模拟进程退出并重新启动。

要求取消后不再输出旧任务 delta；长命令及子进程组被回收；重启只续跑未完成部分；已完成角色、已合并求助和任务报告不重复。

### L07：故障注入（P0）

使用本地 HTTP fake 和受控工具覆盖 401/429/5xx、SSE 半包/无 completed、provider 超时、非法工具参数、工具返回错误、bash 非零退出、输出超限、VFS 合并冲突、非法协调图。

要求错误进入正确层级并可观察；图、记忆和文件不发生半提交；manager 不崩溃，恢复外部条件后可继续接收新消息。

### L08：真实项目任务矩阵（P1）

在全部 5 个固定项目上执行任务清单。每项默认只给一次机会，同时记录 `pass@1`；失败后最多允许两次从全新副本重试，记录 `pass@3`，不能在原污染环境上人工续改后算通过。

重点检查：

- 能否先读项目约束，再定位相关实现、调用方和测试。
- 能否用项目已有工具完成构建和验证，而不是只做文本编辑。
- 大仓库中是否只加载相关上下文，是否会被无关文件拖慢或压爆窗口。
- 两个子任务并行时是否真正隔离，join 后是否保留双方非冲突修改。
- 面对冲突、失败测试或依赖问题时，是否如实报告而不是宣称完成。
- 最终改动是否最小、可维护、无无关文件和敏感信息。

## 6. 压测监控方案

监控准备是压力测试前置门槛。监控没有覆盖到的长压结果不作为验收证据。

### 采集方式

先复用现有 RuntimeEvent、`slog` 日志和进程信息，统一按 run ID 产出 JSONL；指标标签只使用有界枚举，task/agent/run ID 放日志和 trace，不放 Prometheus 高基数标签。每秒采集一次 OS/runtime 指标；发现 CPU、内存、阻塞或泄漏异常时再抓 CPU、heap、goroutine、mutex profile 和 execution trace。

| 层级 | 必采指标 |
| --- | --- |
| 用户请求 | 请求数、成功/失败/取消、排队时长、首字延迟、总耗时、重试次数 |
| 模型 | 请求耗时、TTFT、输入/输出/reasoning/cache token、流式中断、provider 错误分类、估算费用 |
| Agent | ReAct step、tool call、compact 次数、上下文使用率、checkpoint save/load/discard |
| 协调图 | active/pending/completed/canceled task、图宽度/深度、spawn/join/help 次数、节点等待时长 |
| 工具与 exec | 调用次数/耗时/错误、执行队列长度、active slots、进程启动耗时、超时、截断、退出码 |
| VFS/记忆 | materialize/absorb/fork/merge/flush 耗时、字节量、冲突、节点/边/子图数量、存储文件大小 |
| Go/runtime | CPU、RSS/heap、GC 次数和暂停、goroutine、线程、mutex/block、文件描述符 |
| 资源回收 | 子进程组、live 临时目录、checkpoint、progress、磁盘占用在任务结束后的残留量 |

### 在线告警和自动取证

以下条件在压测中实时触发告警；严重项立即停止继续加压并保存现场：

- manager 标记 busy 但 60 秒没有 model/tool/task 事件：疑似死锁或永久等待。
- active exec 超过配置 slots，或本地任务排队超过 30 秒且资源未饱和。
- goroutine、RSS、文件描述符或临时目录连续 3 个五分钟窗口单调增长。
- 任务成功后仍有活跃子进程、未清理 checkpoint/progress，或取消后继续产生旧任务 delta。
- 非注入的内部错误率超过 1%，P95 较固定基线显著恶化超过 20%。
- 日志、patch 或模型记录中命中凭据格式和验收 canary secret。
- CPU、内存、磁盘或模型费用达到测试预算的 80%；停止加压但保留已完成样本。

每轮报告至少包含：吞吐/延迟分位图、任务状态和错误分类、队列与并发曲线、CPU/RSS/goroutine/FD 曲线、token/费用、VFS/记忆规模、清理前后资源差。监控自身通过开/关 A/B 测量，CPU 或时延开销超过 5% 时结果需注明并优化采样频率。

## 7. 主动压力与破坏性场景

压力按阶梯逐级提高，先测容量曲线，再有意打到饱和点；每一级通过或明确失败原因后才进入下一级。

| ID | 压力维度 | 负载阶梯 | 主要观察 |
| --- | --- | --- | --- |
| S01 | 请求突发 | 1/10/50/100 条连续 Send | manager 队列、响应串线、取消与尾延迟 |
| S02 | 协调图 | 宽度 1/4/16/64，深度 1/3/6，总任务至 100 | 调度正确性、spawn/join、内存、饥饿、死锁 |
| S03 | 命令调度 | 100/500/1000 个短命令，slots=1/2/4/8 | 并发上限、排队、公平性、进程回收、吞吐 |
| S04 | 仓库规模 | 1k/10k/50k+ 文件，含大文件和深目录 | materialize、grep/find、上下文选择、磁盘与 RSS |
| S05 | 记忆与长会话 | 1k/10k/100k 节点，20/100/500 轮 | 查询、压缩、持久化、恢复、上下文溢出 |
| S06 | 文件竞争 | 兄弟任务写不同文件、同文件、文件/目录互换 | 隔离、三路 merge、冲突原子性、无静默覆盖 |
| S07 | 故障风暴 | 5%/20% provider/tool 失败、SSE 截断、随机超时 | 错误传播、状态一致性、恢复、告警准确性 |
| S08 | 随机取消/重启 | 在每个角色和工具阶段注入取消或进程退出 | checkpoint、幂等、资源回收、重复副作用 |
| S09 | Soak | 30 分钟预跑、2 小时验收、8 小时扩展 | 慢泄漏、状态漂移、长尾、费用和磁盘增长 |

压力通过标准：没有 panic、race、死锁、越界访问、静默数据丢失或环境串线；所有未注入失败的任务最终进入明确终态；active exec 从不超过 slots；负载撤除后进程、goroutine、FD、临时目录和队列回到稳态范围。达到饱和后允许延迟上升，但不能出现吞吐永久坍塌或取消失效。

## 8. 性能与真实项目质量

首轮在固定机器建立基线。网络模型场景至少 5 次并报告 P50/P90/P95；本地 benchmark 至少 10 次并使用 `benchstat`。基准实验串行运行，不能用一次结果或与压测并行的噪声结果宣称提升/回归。

| 指标 | 工作负载 | 首轮建议门槛 |
| --- | --- | --- |
| 真实项目正确率 | L08 全任务集 | `pass@1 >= 80%`，`pass@3 >= 95%`；同时报告各任务类型 |
| 本地启动时间 | CLI 启动到首个 provider 请求，不含网络 | P95 <= 1s |
| 真实模型首字延迟 | L01 | P95 <= 10s；provider 时间单列 |
| 一次工具 ReAct | L02 | P95 <= 45s，5 次至少 4 次成功 |
| Threadmill 自举任务 | L03 | 在任务预算内完成；受影响测试通过；相同任务 P95 不比基线慢 20% |
| 动态求助任务 | L04 | P95 <= 300s，3 次全部完成且无重复 join |
| 取消延迟 | 长模型请求、长 bash、100 节点任务树 | 本地部分 P95 <= 2s；provider 断连单列 |
| 文件视图 | 1k/10k/50k+ 文件的 materialize/absorb/fork/merge | 记录 sec/op、B/op、allocs/op；显著回归不超过 20% |
| 记忆图 | 1k/10k/100k 节点的查询/fork/merge/flush | 记录 P50/P95、堆内存；显著回归不超过 20% |
| 稳态资源 | S01–S08 后回落 | GC 后 RSS 相对稳态增长 <= 20%；goroutine/FD/子进程/临时目录无净增长 |
| 单任务效率 | 每类真实项目任务 | 报告总时长、模型调用、tool call、token、估算费用及每个成功任务成本 |

采样必须拆出排队、VFS、进程启动、provider 连接、首 token、模型生成、工具、join/merge、持久化时间，避免把 provider 慢误判为 Threadmill 慢。出现性能异常时，以 pprof/trace 定位后再决定是否优化。

## 9. 建议执行顺序

1. **可观测性就绪**：验证事件、结构化日志、OS/runtime 采集、告警和产物目录；测量监控开销。
2. **静态基线**：`go build ./...`、`go test ./...`、`go test -race ./...`；P0 失败先解决。
3. **协议与故障冒烟**：F02、F05–F07、F11–F13、L01、L02、L07。
4. **真实项目功能验收**：先跑 L03 自举，再跑 L08 的 5 个项目任务矩阵。
5. **Agent OS 特性验收**：L04、L05、L06，覆盖子任务、动态求助、记忆和恢复。
6. **容量曲线**：依次执行 S01–S06，找到调度、VFS、记忆和仓库规模的拐点。
7. **故障与恢复压力**：执行 S07、S08，核对错误分类、状态一致性和自动取证。
8. **稳定性**：30 分钟预跑通过后执行 2 小时 soak；8 小时作为扩展验收。
9. **统计复跑与报告**：本地 benchmark 串行复跑，用同一基线比较；汇总真实任务正确率、性能、成本和缺陷。

## 10. 总体验收出口

满足以下条件才可判定本轮通过：

- 所有 P0 用例通过，且 `go test -race ./...` 无失败。
- L01–L08 有完整证据；真实项目达到正确率门槛，结果由项目自身命令和 diff 证明。
- 2 小时压力/soak 无 panic、race、死锁、数据串线、静默覆盖、残留进程或密钥泄漏。
- 压测全过程可关联 run -> task -> agent -> model/tool -> 文件/记忆变更；每个异常均能定位阶段并保留取证。
- 性能达到建议门槛；未达到时有分段耗时、profile 或明确外部 provider 证据。
- 所有失败、豁免和待定产品语义均有责任人及复验条件。

## 11. 当前基线与待确认项

2026-08-20 在 `feat/tui` 工作树重新执行 `go build ./...`、`go vet ./...`、`go test -shuffle=on ./...` 和 `go test -race -shuffle=on ./...`，均通过。先前 `TestSchedulerDoesNotAbsorbOnRun` 与既定的 `Run -> Release -> Absorb` 生命周期矛盾，测试口径已修正，exec 阻断解除。

本轮还用本地 OpenAI-compatible HTTP/SSE 服务跑通了真实二进制主链路，并完成 100 个进程、并发度 20 的突发测试；100/100 均得到 `done` 任务报告和唯一 marker。由于本机没有配置真实供应商凭据，这只能证明真实网络协议、调度、文件工具和持久化链路，不能代替 L01/L02 的外部模型质量、费用和网络尾延迟验收。

当前已有空闲/错误快照，覆盖 model/tool/task/memory 生命周期、TTFT、时延桶、token、exec 排队和槽位、VFS/记忆规模以及 Go heap/goroutine/GC。监控层仍不算完备：Prometheus/OpenTelemetry 导出、run/trace 关联、周期性 CPU/RSS/FD/子进程采样、在线告警和异常自动 profile 仍待实现。

首轮开始前还需确认两项产品语义：

1. 根任务的文件增量最终应合入宿主工作区、保留为可恢复版本，还是只存在于任务环境；不同答案会改变 L03/L06 的落盘标准。
2. provider 级 429/5xx 是否要求 Threadmill 自动重试；当前草案只要求错误可观察、状态不损坏、用户可再次发起。
